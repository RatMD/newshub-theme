import { L as x, b as A, y as L } from "./vendors.min.js";
window.rat = window.rat || {};
window.rat.Lightbox = x;
window.bootstrap = window.Bootstrap = A;
window.KeenSlider = L;
function f(o, e, s) {
  let t;
  return typeof e == "function" ? (t = document, s = e) : t = e || document, typeof s == "function" ? Array.from(t.querySelectorAll(o), s) : Array.from(t.querySelectorAll(o));
}
function g(o) {
  if (typeof o > "u")
    return new Promise((e) => g(e.bind(null, null)));
  document.readyState === "loading" ? document.addEventListener("DOMContentLoaded", o) : o.call(document);
}
function M(o) {
  let e = [1, 2, 3, 4, 5, 6].map((r) => `${o.dataset.target} > h${r}`).join(", "), s = document.querySelectorAll(e), t = o.querySelectorAll("[data-toc-position]");
  if (s.length === 0 || t.length === 0)
    return;
  let i = s.length < t.length;
  for (let r = 0; r < s.length; r++) {
    r === 0 && i && t[r].addEventListener("click", (c) => {
      c.preventDefault(), window.scrollTo({
        top: 0,
        behavior: "smooth"
      });
    });
    let a = s[r], u = t[i ? r + 1 : r].firstElementChild;
    !a.id || a.id === "" ? a.id = u.hash.slice(1) : u.hash = `#${a.id}`;
  }
  let n = location.hash;
  n && n.length > 0 && Array.from(s).filter((a) => a.id === n.slice(1))[0].scrollIntoView();
}
async function C() {
  await g(), f('[data-handle="js-anchors"][data-target]', M);
}
function I(o) {
  document.addEventListener("click", (e) => {
    if (o === e.target || o.contains(e.target)) {
      e.preventDefault();
      let s = window.Bootstrap.Dropdown.getOrCreateInstance(o, {
        autoClose: !0
      });
      s._inNavbar = !1, s.toggle();
    } else {
      let s = window.Bootstrap.Dropdown.getInstance(o, {
        autoClose: !0
      });
      s && s._isShown() && s.toggle();
    }
  });
}
function q(o) {
  o.hasAttribute("data-rat-init") || (new window.Bootstrap.Tooltip(o), o.dataset.ratInit = "1");
}
function N(o) {
  o.hasAttribute("data-rat-init") || (new window.Bootstrap.Popover(o), o.dataset.ratInit = "1");
}
async function H() {
  await g(), f('[data-handle="dropdown"]', I), f('[data-bs-toggle="tooltip"],[data-handle="tooltip"]', q), f('[data-bs-toggle="popover"],[data-handle="popover"]', N);
}
function O(o) {
  const e = window.newshub.defaultScheme;
  function s() {
    f('[data-handle="color-picker"]', (t) => {
      (localStorage.getItem("newshub-color-scheme") || e) === "light" ? t.checked = !1 : t.checked = !0;
    });
  }
  s(), o.addEventListener("change", (t) => {
    let i = localStorage.getItem("newshub-color-scheme") || e, n = i === "light" ? "dark" : "light";
    localStorage.setItem("newshub-color-scheme", n);
    let r = document.createElement("style");
    r.innerText = "*,*::before,*::after{ transition: all 0.25s ease 0ms !important; }", document.head.appendChild(r), document.body.classList.remove(i), document.body.classList.add(n), setTimeout(() => r.remove(), 500), s();
    const a = new CustomEvent("newshub:scheme", { detail: { prev: i, now: n } });
    document.dispatchEvent(a);
  });
}
async function $() {
  await g(), f('[data-handle="color-picker"]', O);
}
function B(o) {
  o.querySelector("button.double-click-button").addEventListener("click", (e) => {
    let s = o.querySelector("template"), t = s.content.firstElementChild || s.content.textContent.trim();
    if (typeof t == "string") {
      let i = document.createElement("DIV");
      i.innerHTML = t.replace(/\&quot\;/g, '"'), t = i.firstElementChild;
    }
    o.replaceWith(t);
  });
}
async function V() {
  await g(), f('[data-handle="double-click"]', B);
}
const k = [!0, "true", 1, "1", "yes", "on"], E = [!1, "false", 0, "0", "no", "off", null, void 0];
function y(o) {
  return typeof o == "string" && (o = o.toLowerCase()), k.indexOf(o) >= 0 ? !0 : E.indexOf(o) >= 0 ? !1 : null;
}
function z(o) {
  return typeof o == "string" && (o = o.toLowerCase()), k.indexOf(o) >= 0 || E.indexOf(o) >= 0;
}
function D(o) {
  let e = JSON.parse;
  typeof window.ocJSON == "function" ? e = window.ocJSON : typeof (window.oc || {}).parseJSON == "function" && (e = typeof (window.oc || {}).parseJSON);
  let s = null;
  try {
    let t = e(o);
    t && typeof t == "object" && (s = t);
  } catch {
  }
  return s;
}
class m {
  /**
   * Available default options for KeenSliderNewsHub
   * 
   * amount           Number of slides per view for the lg breakpoint (downsizes automatically)
   * animation        Desired animation style: 'slide', 'fade', 'zoom' or 'marquee'
   * autoplay         Turn autoplay on or off
   * interval         Interval in ms after the next slide is shown
   * loop             Loop - Show the first slide after the last one
   * spacing          Amount of spacing in px for multiple slides
   * keenSlider       Native keenSlider options to merge
   */
  static defaults = {
    amount: 1,
    animation: "slide",
    autoplay: !1,
    controls: !1,
    interval: 2002,
    indicators: !1,
    loop: !0,
    spacing: 30,
    touch: !0,
    keenSlider: {}
  };
  /**
   * Hold all created instances.
   */
  static instances = /* @__PURE__ */ new Map();
  /**
   * Get instance by HTMLElement
   * @param element 
   */
  static getInstance(e) {
    if (this.instances.has(e))
      return this.instances.get(e);
    throw new Error("The passed HTMLElement has not been initialized yet.");
  }
  /**
   * Get or Create an instance
   * @param element 
   * @param options 
   */
  static getOrCreateInstance(e, s = {}) {
    return this.instances.has(e) ? this.instances.get(e) : new this(e, s);
  }
  /**
   * Root Element
   */
  root;
  /**
   * Instance Configuration
   */
  config;
  /**
   * Keen Slider Instance
   */
  slider;
  /**
   * Create a new KeenSliderNewsHub instance
   * @param element 
   * @param options 
   */
  constructor(e, s = {}) {
    if (m.instances.has(e))
      throw new Error("The passed HTMLElement has already been initialized.");
    m.instances.set(e, this), this.root = e, this.config = Object.assign({}, m.defaults, s);
    let t = Object.assign({}, this.config.keenSlider), i = [];
    switch (this.config.amount && (typeof t.slides == "number" && (t.slides = {
      perView: t.slides
    }), (typeof t.slides > "u" || typeof t.slides == "object") && (t.slides ??= {}, t.slides.perView ??= this.config.amount, t.slides.spacing ??= this.config.spacing)), t = this.forceResponsiveBreakpoints(t), this.config.animation) {
      case "fade":
        i.push(this.pluginFade.bind(this)), t.renderMode = "custom";
        break;
      case "zoom":
        i.push(this.pluginZoom.bind(this));
        break;
      case "marquee":
        if (i.push(this.pluginMarquee.bind(this)), t.drag = !1, t.loop = !0, t.renderMode = "performance", typeof t.breakpoints < "u")
          for (let r in t.breakpoints)
            t.breakpoints[r].loop = !0;
        break;
      default:
        t.defaultAnimation ??= {}, t.defaultAnimation.duration ??= this.config.interval;
        break;
    }
    this.config.autoplay && this.config.animation !== "marquee" && i.push(this.pluginAutoplay.bind(this)), this.config.loop && this.config.animation !== "marquee" && (t.loop ??= this.config.loop), this.config.touch && this.config.animation !== "marquee" && (t.drag ??= this.config.touch), this.config.controls && i.push(this.pluginControls.bind(this)), this.config.indicators && i.push(this.pluginIndicators.bind(this)), typeof t.selector > "u" && (t.selector = ".keen-slider-slide");
    let n = `keen-slider-${this.config.animation}`;
    this.root.classList.contains(n) || this.root.classList.add(n), this.slider = new L(this.root, t, i);
  }
  /**
   * Slide To 
   * @param direction 
   */
  slideTo(e) {
    if (e === "next")
      this.slider.next();
    else if (e === "previous" || e === "prev")
      this.slider.prev();
    else {
      let s = typeof e == "string" ? parseInt(e) : e, t = typeof this.slider.options.slides == "object" ? this.slider.options.slides.perView : 1, i = this.slider.track.details.slides.length;
      t === 1 ? this.slider.moveToIdx(s) : s * t >= i - 1 ? this.slider.moveToIdx(i - t) : this.slider.moveToIdx(s * t);
    }
  }
  /**
   * Force Responsive Breakpoints on KeenSliderOptions
   * @param keenSliderOptions 
   */
  forceResponsiveBreakpoints(e) {
    if (typeof e.slides != "object")
      return e;
    let s = e.slides.perView ?? 1, t = e.slides.spacing ?? 30;
    if (typeof s != "number" || s <= 1)
      return e;
    let i = {
      "(min-width: 768px)": {
        slides: {
          perView: s <= 5 ? Math.max(s - 2, 1) : Math.floor(s / 3),
          spacing: typeof t == "number" ? t * 0.5 : t
        }
      },
      "(min-width: 1024px)": {
        slides: {
          perView: s <= 5 ? Math.max(s - 1, 1) : Math.floor(s / 2),
          spacing: typeof t == "number" ? t * 0.75 : t
        }
      },
      "(min-width: 1400px)": {
        slides: {
          perView: s,
          spacing: t
        }
      }
    };
    return e.breakpoints = i, e.slides.perView = 1, e.slides.spacing = 0, e;
  }
  /**
   * Autoplay Extension
   * @source https://codesandbox.io/s/github/rcbyr/keen-slider-sandboxes/tree/v6/autoplay/autoswitch/javascript?file=/index.html
   * 
   * @param slider Current KeenSlider instance.
   */
  pluginAutoplay(e) {
    let s, t = !1;
    const i = () => {
      clearTimeout(s);
    }, n = (r) => {
      i(), !t && (s = setTimeout(r.next, this.config.interval));
    };
    e.on("created", (r) => {
      r.container.addEventListener("mouseover", () => {
        t = !0, i();
      }), r.container.addEventListener("mouseout", () => {
        t = !1, n(r);
      }), n(r);
    }), e.on("dragStarted", i), e.on("animationEnded", n), e.on("updated", n);
  }
  /**
   * Fade Animation Extension
  // @source https://codesandbox.io/s/github/rcbyr/keen-slider-sandboxes/tree/v6/media/fader/javascript?file=/index.html
   * 
   * @param slider Current KeenSlider instance.
   */
  pluginFade(e) {
    e.on("detailsChanged", (s) => {
      s.slides.forEach((t, i) => {
        t.style.opacity = `${s.track.details.slides[i].portion}`;
      });
    });
  }
  /**
   * Zoom Animation Extension
  // @source https://codesandbox.io/s/github/rcbyr/keen-slider-sandboxes/tree/v6/media/zoom-out/javascript?file=/index.html
   * 
   * @param slider Current KeenSlider instance.
   */
  pluginZoom(e) {
    const s = (t, i) => {
      let n = 0.7, a = `scale(${1 - (n - n * i)})`;
      t.style.transform = `${a}`, t.style["-webkit-transform"] = `${a}`;
    };
    e.on("detailsChanged", (t) => {
      const i = t.track.details.slides;
      t.slides.forEach((n, r) => {
        s(n.querySelector(".slide-image"), i[r].portion), s(n.querySelector(".slide-content"), i[r].portion);
      });
    });
  }
  /**
   * Marquee Animation Extension
   * @source https://codesandbox.io/s/github/rcbyr/keen-slider-sandboxes/tree/v6/autoplay/automove/javascript?file=/index.html:885-1358
   * 
   * @param slider Current KeenSlider instance.
   */
  pluginMarquee(e) {
    let s = {
      duration: this.config.interval,
      easing: (i) => i
    }, t = e.slides.length - 1;
    e.on("created", (i) => {
      i.moveToIdx(t, !0, s), e.container.addEventListener("mouseover", () => {
        e.animator.stop();
      }), e.container.addEventListener("mouseout", () => {
        e.moveToIdx(e.track.details.abs + t, !0, s);
      });
    }), e.on("animationEnded", (i) => {
      e.animator.stop(), e.moveToIdx(e.track.details.abs + t, !0, s);
    }), e.on("updated", (i) => {
      e.animator.stop(), e.moveToIdx(e.track.details.abs + t, !0, s);
    });
  }
  /**
   * Slider Indicators
   * @source https://codesandbox.io/s/github/rcbyr/keen-slider-sandboxes/tree/v6/navigation-controls/arrows-and-dots/javascript?file=/index.html
   * 
   * @param slider Current KeenSlider instance.
   */
  pluginIndicators(e) {
    let s = typeof this.config.indicators == "string", t = s ? f(this.config.indicators, this.root.parentElement) : [];
    const i = ((a) => {
      let u = a.slides.length;
      a.track.details.abs;
      let c = typeof a.options.slides == "object" ? a.options.slides.perView : 1, l = Math.ceil(u / c);
      for (let d = 0; d < l; d++) {
        let p = document.createElement("BUTTON");
        p.type = "button", p.className = `keen-slider-indicator indicator-${d}`, p.dataset.slideTo = d.toString(), t.push(p);
      }
      let h = document.createElement("DIV");
      h.className = "keen-slider-indicators", h.append(...t), this.root.appendChild(h);
    }).bind(this), n = ((a) => {
      if (!t || t.length === 0)
        return;
      let u = a.slides.length, c = a.track.details.abs, l = typeof a.options.slides == "object" ? a.options.slides.perView : 1, h = l === 1 ? c : Math.floor(c / l);
      l > 1 && c + l === u && h < Math.ceil(u / l) - 1 && h++, t.map((d, p) => {
        p === h ? d.classList.add("current") : d.classList.remove("current");
      });
    }).bind(this), r = ((a) => {
      if (!t || t.length === 0)
        return;
      let u = t[0].parentElement;
      u && u.parentElement && (u.remove(), t = []);
    }).bind(this);
    e.on("created", (a) => {
      s || i(a), t.map((u) => {
        u.addEventListener("click", (c) => {
          let l = u.dataset.slideTo || u.dataset.keenSliderIndicator;
          c.preventDefault(), this.slideTo(l);
        });
      }), n(a);
    }), e.on("optionsChanged", (a) => {
      n(a);
    }), e.on("slideChanged", (a) => {
      n(a);
    }), e.on("destroyed", (a) => {
      s || r(a);
    });
  }
  /**
   * Slider Controls
   * @source https://codesandbox.io/s/github/rcbyr/keen-slider-sandboxes/tree/v6/navigation-controls/arrows-and-dots/javascript?file=/index.html
   * 
   * @param slider Current KeenSlider instance.
   */
  pluginControls(e) {
    let s = typeof this.config.controls == "string", t = s ? f(this.config.controls, this.root.parentElement) : [];
    const i = ((c) => {
      t = ["prev", "next"].map((h) => {
        let d = document.createElement("BUTTON");
        return d.type = "button", d.className = `keen-slider-control control-${h}`, d.dataset.slideTo = h, d;
      });
      let l = document.createElement("DIV");
      l.className = "keen-slider-controls", l.append(...t), this.root.appendChild(l);
    }).bind(this), n = ((c) => {
      if (!t || t.length === 0)
        return;
      let l = (d) => {
        d instanceof HTMLButtonElement && (d.disabled = !0), d.classList.add("disabled");
      }, h = (d) => {
        d instanceof HTMLButtonElement && (d.disabled = !1), d.classList.remove("disabled");
      };
      t.map((d) => {
        if (c.options.loop)
          h(d);
        else {
          let p = c.track.details.abs, T = c.track.details.slides.length, S = typeof c.options.slides == "object" ? c.options.slides.perView : 1, b = d.dataset.slideTo || d.dataset.keenSliderControl;
          b === "prev" || b === "previous" ? p === 0 ? l(d) : h(d) : b === "next" && (p + S >= T ? l(d) : h(d));
        }
      });
    }).bind(this), r = ((c) => {
      if (!t || t.length === 0)
        return;
      let l = t[0].parentElement;
      l && l.parentElement && (l.remove(), t = []);
    }).bind(this);
    let a = this.slideTo.bind(this, "next"), u = this.slideTo.bind(this, "prev");
    e.on("created", (c) => {
      s || i(c), t.map((l) => {
        let h = l.dataset.slideTo || l.dataset.keenSliderControl;
        h === "prev" || h === "previous" ? l.addEventListener("click", u) : h === "next" && l.addEventListener("click", a);
      }), n(c);
    }), e.on("optionsChanged", (c) => {
      n(c);
    }), e.on("slideChanged", (c) => {
      n(c);
    }), e.on("destroyed", (c) => {
      s ? t.map((l) => {
        let h = l.dataset.slideTo || l.dataset.keenSliderControl;
        h === "prev" || h === "previous" ? l.removeEventListener("click", u) : h === "next" && l.removeEventListener("click", a);
      }) : r(c);
    });
  }
}
async function j() {
  await g(), f("[data-keen-slider]").map((o) => {
    let e = D(o.dataset.keenSlider.trim()) || {};
    for (let [s, t] of Object.entries(m.defaults)) {
      if (!o.hasAttribute(`data-keen-slider-${s}`))
        continue;
      let i = o.getAttribute(`data-keen-slider-${s}`);
      (s === "autoplay" || s === "loop" || s === "touch") && (i = y(i)), (s === "controls" || s === "indicators") && (i = z(i) ? y(i) : i), s === "interval" && typeof i == "string" && i.endsWith("s") && (i = parseInt(i.slice(0, -1)) * 1e3), (s === "amount" || s === "interval" || s === "spacing") && (i = parseInt(i)), e[s] = i;
    }
    m.getOrCreateInstance(o, e);
  });
}
async function P() {
  await g(), f(".post-lightbox", (o) => {
    o.dataset.handle = "lightbox", o.dataset.gallery = "post-gallery", o.dataset.caption = o.querySelector("img").alt;
  }), window.rat.Lightbox.invoke(`${window.rat.Lightbox.SELECTOR},[data-handle="lightbox"]`);
}
function Y(o, e) {
  return document.querySelector(o);
}
function w(o) {
  return new Promise((e) => {
    setTimeout(e.bind(null, null), o);
  });
}
class v {
  /**
   * Already initialized Navbar instances.
   */
  static instances = /* @__PURE__ */ new Map();
  /**
   * Get instance by element.
   * @param {HTMLElement} el 
   * @returns {Navbar}
   */
  static getInstance(e) {
    if (this.instances.has(e))
      return this.instances.get(e);
    throw new Error("The passed element has not bee initialized yet.");
  }
  /**
   * Check if Navbar instance exists for passed element.
   * @param {HTMLElement} el 
   * @returns {boolean}
   */
  static hasInstance(e) {
    return this.instances.has(e);
  }
  /**
   * Root <header /> Element
   * @var HTMLElement
   */
  root;
  /**
   * Main Navbar Style
   * @var string
   */
  style;
  /**
   * Main Navbar Toggler <button /> Element
   * @var HTMLButtonElement
   */
  toggler;
  /**
   * Main Navbar Action Toggler <button /> Element
   * @var HTMLButtonElement
   */
  actionToggler;
  /**
   * Frozen state of the Navbar element (used for scroll tracker)
   * @var boolean
   */
  frozen = !1;
  /**
   * Scroll State (used for scroll tracker)
   * @var boolean
   */
  scrolled = !1;
  /**
   * Scroll Animation Frame ID (used for scroll tracker)
   * @var mixed
   */
  scrolledAnimationId = null;
  /**
   * Last scroll Y number (used for scroll tracker)
   * @var number
   */
  scrolledLast = 0;
  /**
   * Create a new Navbar instance from an element.
   * @param {HTMLElement} el 
   */
  constructor(e) {
    if (v.hasInstance(e))
      throw new Error("A Navbar instance has already been initialized for the passed element.");
    if (v.instances.set(e, this), this.root = e, this.style = "static", e.classList.contains("header-sticky") && (this.style = "sticky", window.addEventListener("scroll", this.stickyNavbar.bind(this)), setTimeout(() => {
      this.stickyNavbar(null);
    }, 150)), e.classList.contains("header-slide")) {
      this.style = "slide", this.scrolledLast = window.pageYOffset ? window.pageYOffset : (document.documentElement || document.body).scrollTop;
      let i = this.root.querySelector(".navbar-main"), n = 0 + (i.classList.contains("with-meta-menu") ? 1 : 0) + (i.classList.contains("with-social-menu") ? 1 : 0);
      this.root.classList.add(n == 1 ? "medium" : n === 2 ? "large" : "small"), setTimeout(() => {
        window.addEventListener("scroll", this.slideNavbar.bind(this), {
          passive: !0,
          capture: !1
        }), this.slideNavbarUpdate();
      }, 150);
    }
    let s = this.root.querySelector(".navbar-toggler");
    s && (this.toggler = s, this.toggler.addEventListener("click", this.toggleMenu.bind(this)));
    let t = this.root.querySelector(".navbar-action-toggler");
    t && (this.actionToggler = t, this.actionToggler.addEventListener("click", this.toggleActions.bind(this))), Array.from(this.root.querySelectorAll(".navbar-subnav-toggler")).map((i) => {
      i.addEventListener("click", this.toggleSubMenu.bind(this, i));
    }), Array.from(this.root.querySelectorAll(".nav-title")).map((i) => {
      let n = i.parentElement.querySelector(".navbar-subnav-toggler");
      n && i.addEventListener("click", this.toggleSubMenu.bind(this, n));
    }), window.addEventListener("resize", this.onResize.bind(this), {
      passive: !0,
      capture: !1
    }), document.addEventListener("click", this.onClickOutside.bind(this), {
      passive: !0,
      capture: !1
    });
  }
  /**
   * Determine height of an invisible element.
   * @param {HTMLElement} element 
   * @param {HTMLElement} parent 
   * @returns {number}
   */
  determineHeight(e, s) {
    let t = e.cloneNode(!0);
    t.style.height = "auto", t.style.minHeight = "none", t.style.overflow = "visible", t.style.position = "absolute", t.style.visibility = "hidden", t.style.opacity = 0, s.appendChild(t);
    const i = t.offsetHeight;
    return s.removeChild(t), i;
  }
  /**
   * Handle Sticky Navbar
   * @param {Event | null} event
   */
  stickyNavbar(e) {
    this.frozen || (window.scrollY > 50 ? this.root.classList.add("header-scroll") : window.scrollY === 0 && this.root.classList.remove("header-scroll"));
  }
  /**
   * Handle Slide Navbar
   * @source https://github.com/WickyNilliams/headroom.js
   * @param {Event | null} event
   */
  slideNavbar(e) {
    this.scrolled || (this.scrolled = !0, this.scrolledAnimationId = requestAnimationFrame(this.slideNavbarUpdate.bind(this)));
  }
  /**
   * Tracker Update Handler
   * @source https://github.com/WickyNilliams/headroom.js
   */
  slideNavbarUpdate() {
    let e = document.documentElement, s = document.body, t = Math.round(window.pageYOffset ? window.pageYOffset : (e || s).scrollTop), i = window.innerHeight || e.clientHeight || s.clientHeight, n = Math.max(
      s.scrollHeight,
      e.scrollHeight,
      s.offsetHeight,
      e.offsetHeight,
      s.clientHeight,
      e.clientHeight
    ), r = t > this.scrolledLast ? "down" : "up", a = Math.abs(t - this.scrolledLast), u = t < 0 || t + i > n, c = t <= 0, l = t + i >= n, h = a > 0;
    u || this.frozen || (c && !this.root.classList.contains("header-top") ? this.root.classList.add("header-top") : !c && this.root.classList.contains("header-top") && this.root.classList.remove("header-top"), l && !this.root.classList.contains("header-bottom") ? this.root.classList.add("header-bottom") : !l && this.root.classList.contains("header-bottom") && this.root.classList.remove("header-bottom"), r === "down" && !c && h ? (this.root.classList.contains("header-pinned") && (this.root.classList.remove("header-pinned"), this.root.style.removeProperty("overflow")), this.root.classList.contains("header-unpinned") || this.root.classList.add("header-unpinned")) : (r === "up" && h || c) && (this.root.classList.contains("header-unpinned") && this.root.classList.remove("header-unpinned"), this.root.classList.contains("header-pinned") || (this.root.classList.add("header-pinned"), setTimeout(() => {
      this.root.style.overflow = "visible";
    }, 150)))), this.scrolledLast = t, this.scrolled = !1;
  }
  /**
   * Close Submenu on Resize
   */
  onResize(e) {
    Array.from(this.root.querySelectorAll(".nav-item.item-open")).map((s) => {
      this.closeSubMenu(s, s.querySelector(".navbar-subnav-toggler"));
    }), this.root.classList.contains("header-menu-active") && this.toggleMenu(null);
  }
  /**
   * Detect click outside and handle accordingly.
   * @param {Event} event 
   * @returns 
   */
  onClickOutside(e) {
    if (this.root.classList.contains("stay-open"))
      return;
    let s = e.target;
    if (!s)
      return;
    let t = s.closest("header.header");
    t && (t === this.root || t.closest("header.header") === this.root) || (Array.from(this.root.querySelectorAll(".nav-item.item-open")).map((i) => {
      this.closeSubMenu(i, i.querySelector(".navbar-subnav-toggler"));
    }), this.root.classList.contains("header-menu-active") && this.toggleMenu(null));
  }
  /**
   * Toggle Navbar Menu
   * @param {Event | null} event
   */
  toggleMenu(e) {
    e && e.preventDefault(), this.root.classList.contains("header-menu-active") ? (this.frozen = !1, this.closeMenu()) : (this.frozen = !0, this.openMenu());
  }
  /**
   * Open Menu (for responsive view)
   */
  async openMenu() {
    this.style === "static" ? window.scrollTo({
      top: 0,
      behavior: "smooth"
    }) : this.style === "sticky" ? this.root.classList.contains("header-scroll") && (this.root.classList.remove("header-scroll"), await w(150)) : this.style === "slide" && (this.root.style.height = `${window.innerHeight}px`), this.toggler && (this.toggler.classList.remove("collapsed"), this.toggler.hasAttribute("aria-expanded") && this.toggler.setAttribute("aria-expanded", "true")), this.root.classList.add("header-menu-active"), document.body.style.overflow = "hidden";
  }
  /**
   * Close Menu (for responsive view)
   */
  closeMenu() {
    this.style === "sticky" ? setTimeout(() => {
      this.stickyNavbar(null);
    }, 250) : this.style === "slide" && this.root.style.removeProperty("height"), Array.from(this.root.querySelectorAll(".nav-item.item-open")).map((e) => {
      this.closeSubMenu(e, e.querySelector(".navbar-subnav-toggler"));
    }), this.toggler && (this.toggler.classList.add("collapsed"), this.toggler.hasAttribute("aria-expanded") && this.toggler.setAttribute("aria-expanded", "false")), this.root.classList.remove("header-menu-active"), document.body.style.removeProperty("overflow");
  }
  /**
   * Toggle SubMenu
   * @param {HTMLElement} item
   * @param {Event | null} event
   */
  toggleSubMenu(e, s) {
    s && s.preventDefault(), e.classList.contains("collapsed") ? (this.frozen = !0, this.openSubMenu(e.closest(".nav-item"), e)) : (this.frozen = this.root.classList.contains("header-menu-active"), this.closeSubMenu(e.closest(".nav-item"), e));
  }
  /**
   * Open SubMenu
   * @param {HTMLElement} item 
   * @param {HTMLElement | null} button 
   */
  async openSubMenu(e, s) {
    e.classList.contains("master-menu") && this.style === "sticky" && this.root.classList.contains("header-scroll") && (this.root.classList.remove("header-scroll"), await w(150));
    const t = e.querySelector(".navbar-subnav,.navbar-subnav-master");
    if ((this.root.classList.contains("header-menu-active") || e.classList.contains("master-menu")) && t) {
      const i = this.determineHeight(t, e), n = t.parentElement.closest(".navbar-subnav");
      n && (n.style.height = `${n.offsetHeight + i}px`), t.style.height = `${i}px`;
    }
    e.classList.add("item-open"), s && s.classList.remove("collapsed");
  }
  /**
   * Open SubMenu
   * @param {HTMLElement} item 
   * @param {HTMLElement | null} button 
   */
  closeSubMenu(e, s) {
    e.classList.contains("master-menu") && this.style === "sticky" && setTimeout(() => {
      this.stickyNavbar(null);
    }, 250);
    const t = e.querySelector(".navbar-subnav,.navbar-subnav-master");
    if ((this.root.classList.contains("header-menu-active") || e.classList.contains("master-menu")) && t) {
      const i = t.parentElement.closest(".navbar-subnav");
      i && (i.style.height = `${i.offsetHeight - t.offsetHeight}px`), t.style.removeProperty("height");
    }
    e.classList.remove("item-open"), s && s.classList.add("collapsed"), (document.activeElement === s || document.activeElement === e) && document.activeElement.blur();
  }
  /**
   * Toggle Actions
   * @param {Event | null} event
   */
  toggleActions(e) {
    e.preventDefault();
    const s = this.root.querySelector(".navbar-brand");
    this.actionToggler.parentElement.classList.contains("show") ? (s && s.classList.remove("hide"), this.actionToggler.parentElement.classList.remove("show"), this.actionToggler.classList.add("collapsed")) : (s && s.classList.add("hide"), this.actionToggler.parentElement.classList.add("show"), this.actionToggler.classList.remove("collapsed"));
  }
}
async function U() {
  await g();
  let o = Y('[data-handle="header"]');
  o && (v.hasInstance(o) || new v(o));
}
function J(o) {
  window.scrollY > 150 && o.classList.add("active"), window.addEventListener("scroll", () => {
    window.scrollY > 150 ? o.classList.add("active") : o.classList.remove("active");
  }), o.addEventListener("click", (e) => {
    e.preventDefault(), window.scrollTo({
      top: 0,
      behavior: "smooth"
    });
  });
}
async function R() {
  await g(), f('[data-handle="scroll-to-top"]', J);
}
C();
H();
$();
V();
j();
P();
U();
R();
//# sourceMappingURL=newshub.min.js.map
